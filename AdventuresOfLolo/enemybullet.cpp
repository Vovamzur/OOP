//#include "enemybullet.h"

//EnemyBullet::EnemyBullet(int x, int y) : Bullet(x, y, 1, 0) {}

//EnemyBullet::~EnemyBullet() {}

//void EnemyBullet::Draw(QPainter& p) {
//    QPixmap pix(":/imgs/img/fire.png");
//    int w = p.device()->width()/13;
//    int h = p.device()->height()/14;
//    int x = static_cast<int>(w*(1+this->m_x));
//    int y = static_cast<int>(h*(2+this->m_y));
//    p.drawPixmap(x, y, w, h, pix);
//}
